自动生成  工具类

1. 按照提示输入项目包路径，注意最后那个 . 不要忘记

2. 该程序有个问题：
注意程序 315/324/326行

      pointer += (line.getBytes().length + 1);
      pointer = pointer - 2;
      pointer = pointer - 1;

      如果  +1  -2  -1  时有报错情况
      就改成  +2  -3   -2